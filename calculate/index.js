const { x, calculateSum } = require("./sum");
const { calculateMultiply } = require("./multiply");

module.exports = { x, calculateSum, calculateMultiply }; // Exporting the functions and variables for use in other files
